# ─────────────────────────────────────────────────────────────
# Swahili Sentiment Analysis – Python Requirements
# Install:  pip install -r requirements.txt
# Python:   >= 3.9
# ─────────────────────────────────────────────────────────────

# ── Core data science ─────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11

# ── Visualisation ─────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12

# ── NLP utilities ─────────────────────────────────────────────
emoji>=2.8               # Emoji stripping in preprocessing

# ── Transformer stack (optional but recommended) ──────────────
# Needed for: src/transformer_models.py, src/augmentation.py
torch>=2.0               # PyTorch; install CUDA variant separately if needed
transformers>=4.40       # HuggingFace Transformers
sentencepiece>=0.1.99    # SentencePiece tokeniser (AfriBERTa, XLM-R)
accelerate>=0.27         # HuggingFace Accelerate (Trainer backend)
datasets>=2.18           # HuggingFace Datasets (optional, for loading AfriSenti directly)

# ── Model serialisation ────────────────────────────────────────
joblib>=1.3              # Saving sklearn models

# ── Notebooks ─────────────────────────────────────────────────
jupyter>=1.0
ipykernel>=6.0
ipywidgets>=8.0          # Progress bars in notebooks
tqdm>=4.66

# ── Optional: experiment tracking ─────────────────────────────
# wandb>=0.16            # Uncomment to enable W&B logging
